Optional legacy FFmpeg location.

The preferred portable location is:
  <ARTBatchEncoder.exe folder>\ffmpeg\ffmpeg.exe

ART Batch Encoder checks that preferred folder first, then the saved/manual path,
then ffmpeg.exe beside the executable, tools folders, and Windows PATH.
